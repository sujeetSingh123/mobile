import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginLeft: 20,
    paddingBottom: 280,
  },

  marginTop: {
    marginTop: 50,
  },
  titleContainer: {
    paddingBottom: 20,
  },
});
