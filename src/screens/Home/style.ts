import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    paddingBottom: 140,
  },
  paddingBottom: {
    paddingBottom: 10,
  },
  marginTop: {
    marginTop: 50,
  },
});
