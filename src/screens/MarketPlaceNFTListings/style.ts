import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginLeft: 20,
    marginTop: 20,
    marginBottom: 20,
  },

  marginTop: {
    marginTop: 30,
  },

  titleContainer: {
    paddingBottom: 20,
  },
});
